{# templates/_search_bar.html.twig #}
<form action="{{ path('handle_search') }}" method="{{ form.vars.method }}" class="global-search-form">
    {{ form_widget(form.query) }}
    <button type="submit" class="btn btn-primary">
        <i class="fas fa-search"></i>
    </button>
</form>
