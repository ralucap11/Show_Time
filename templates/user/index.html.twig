<h1>Users </h1>

<ul>
    {% for user in users %}
        <li>
            <a href="{{ path('app_user_show', {id: user.id}) }}">
                {{ user.email }}
            </a>
        </li>
    {% else %}
        <li>No users found</li>
    {% endfor %}
</ul>
